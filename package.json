{
  "name": "@sdescarries/auth0cli",
  "description": "Auth0 Client for the Resource Owner Password API",
  "version": "1.0.4",
  "license": "MIT",
  "files": [
    "**/*.js",
    "**/*.ts"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "main": "index.js",
  "scripts": {
    "auth0cli": "deno run -A ./auth0cli.ts",
    "build": "deno run -A ./build.ts",
    "test": "deno test -A --coverage=results/coverage",
    "udd": "udd deps/* --tests='deno test -A'"
  },
  "devDependencies": {
    "typescript": "4.3.2"
  }
}
